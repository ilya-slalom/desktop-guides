<Window
    x:Class="DesktopGuides.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="28" FontWeight="SemiBold" Text="Desktop Guides — P0 reader probe" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
            <ComboBox x:Name="FixturePicker"
                      Header="Fixture"
                      Width="220"
                      DisplayMemberPath="Id" />
            <ComboBox x:Name="EncodingPicker" Header="Text encoding" Width="155">
                <x:String>UTF-8 (automatic)</x:String>
                <x:String>CP437</x:String>
                <x:String>Windows-1252</x:String>
            </ComboBox>
            <Button Content="Open" Click="OpenClicked" />
            <Button Content="Capture" Click="CaptureClicked" />
            <Button Content="Restore" Click="RestoreClicked" />
            <Button Content="Smaller" Click="SmallerClicked" />
            <Button Content="Larger" Click="LargerClicked" />
        </StackPanel>
        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Text="Select a fixture and open it."
                   TextWrapping="Wrap" />
        <ContentControl x:Name="ReaderHost"
                        Grid.Row="3"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch" />
    </Grid>
</Window>
